  spring:
    datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/mapo?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: 12341234

    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          show_sql: false
          format_sql: false

    mail:
        host: smtp.gmail.com
        port: 587
        username: bmchhk13@gmail.com
        password: rrdvlpjpjtmyjlnd
        properties:
          mail:
            smtp:
              auth: true
              starttls:
                enable: true
    mvc:
      throw-exception-if-no-handler-found: true
    web:
      resources:
        add-mappings: false

  jwt:
    header: Authorization
    secret: fklsjfpelksakdk@!@!@@1asda
    access-expirationMs: 3600000
    refresh-expirationMs : 2629800000

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  file:
    dir: C:/Users/DeliciousNacho/fileUploadTest/
    path: ${file.uri_path}/**
    api_path: http://localhost:${server.port}${file.uri_path}/
    uri_path: /file

  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace

  server:
    error:
      include-exception: false
      include-stacktrace: never
      include-message: always
      include-binding-errors: always
    port: 8080

  springdoc:
    api-docs:
      path: /api-docs
    default-consumes-media-type: application/json
    default-produces-media-type: application/json
    swagger-ui:
      operations-sorter: method
      tags-sorter: alpha
      path: /swagger-ui.html
      disable-swagger-default-url: true
      display-query-params-without-oauth2: true
    paths-to-match:
      - /api/**
      - /test/**
      - /token

